name: CI for game 2048
on:
  push:
    branches:
      - master
  workflow_dispatch:

env:
  IMAGE_NAME: 2048
  REGISTRY: docker.io

jobs:
  build-and-push:
    name: Build and push docker images
    runs-on: ubuntu-latest
    steps:
      - name: checkout source code
        uses: actions/checkout@v4

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build and Push Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: |
            ${{ secrets.DOCKER_USERNAME }}/${{ env.IMAGE_NAME }}:${{ github.sha }}
            ${{ secrets.DOCKER_USERNAME }}/${{ env.IMAGE_NAME }}:latest

  deploy:
    name: Deploy to production vm
    needs: build-and-push
    runs-on: ubuntu-latest
    steps:
      - name: Deploy via SSH
        uses: appleboy/ssh-action@v1.0.3
        env:
          IMAGE: ${{ secrets.DOCKER_USERNAME }}/${{ env.IMAGE_NAME }}:${{ github.sha }}
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SERVER_SSH_KEY }}
          envs: IMAGE
          script: |
            echo "Deploying image: $IMAGE"
            docker pull $IMAGE

            # 1. Stop and remove ANY existing container using our name or port
            # We do this first to free up port 8080
            docker stop 2048-blue || true
            docker rm 2048-blue || true
            docker stop 2048-green || true
            docker rm 2048-green || true

            # 2. Run the NEW container as 'blue' immediately
            # (Since we are on a single VM, we don't need to rename)
            docker run -d \
              --name 2048-blue \
              -p 8080:80 \
              --log-driver json-file \
              --log-opt max-size=10m \
              --log-opt max-file=3 \
              --restart always \
              $IMAGE

            echo "Deployment successful on port 8080"
